{
  "name": "transformerhub-workspace",
  "private": true,
  "version": "0.1.0",
  "description": "pnpm workspace for TransformerHub monorepo",
  "packageManager": "pnpm@10.14.0",
  "scripts": {
    "dev": "pnpm -r --parallel dev",
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "lint": "pnpm -r lint"
  }
}
